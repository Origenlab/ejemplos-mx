---
// ============================================================================
// /modulos/category-detail — PÁGINA DEL MÓDULO «Categoría a fondo».
// ----------------------------------------------------------------------------
// Octava de la serie /modulos/* (topbar · header · breadcrumbs · hero ·
// section-menu · section-heading · category-card · category-detail), MISMO
// molde (docs/MODULOS.md). NOTA propia de la categoría a fondo: a diferencia
// de la tarjeta (vitrina, N por rejilla), este módulo es la AMPLIACIÓN de UNA
// categoría justo donde la tarjeta no alcanza —dos columnas con info detallada
// + galería de apoyo, para reforzar la categoría antes de entrar a su página—.
// La regla canónica del proyecto, dura: TODOS los bloques category-detail van
// IDÉNTICOS (info izq · galería der), SIN zig-zag. La prop `reverse` existe en
// el componente como antipatrón documentado, no para usarse en el sitio.
// «Eat your own dog food»: el bloque «cada módulo, por dentro» del L2 ya usa
// este componente, así que cada módulo de la serie obtiene su CategoryDetail
// data-driven desde AFONDO[slug] sin tocar la página.
// ============================================================================
import PageLayout from '@layouts/PageLayout.astro'
import Hero from '@components/Hero.astro'
import SectionMenu from '@components/SectionMenu.astro'
import GaleriaDisenos from '@components/GaleriaDisenos.astro'
import DisenoCard from '@components/DisenoCard.astro'
import MarcoMovil from '@components/MarcoMovil.astro'
import Receta from '@components/Receta.astro'
import GuiaNota from '@components/GuiaNota.astro'
import CategoryDetail from '@components/CategoryDetail.astro'
import ModSection from '@components/modulos/ModSection.astro'
import ModBenefits from '@components/modulos/ModBenefits.astro'
import ModAnatomy from '@components/modulos/ModAnatomy.astro'
import ModResponsive from '@components/modulos/ModResponsive.astro'
import ModPattern from '@components/modulos/ModPattern.astro'
import ModProse from '@components/modulos/ModProse.astro'
import ModDosDonts from '@components/modulos/ModDosDonts.astro'
import { WA_MESSAGES, waUrl } from '@config/site'
import { siblingsModules } from '@lib/modules'

// Menú de cierre — derivado de MODULOS (SSoT) con el helper siblingsModules:
// «el menú reparte; el último botón —WhatsApp— convierte». La página de cada
// módulo enlaza al índice de la serie + sus dos vecinos en estado 'listo' + la
// home, sin escribir los enlaces a mano (los 'proximo' aún no tienen página).
const cierreItems = siblingsModules('category-detail')
const cierreCta = { label: 'Cotizar por WhatsApp', href: waUrl(WA_MESSAGES.cotizar), sub: 'Respuesta inmediata', external: true }

// Las 4 piezas del bloque a fondo — mapean con los pines del ejemplo en vivo.
// Cada nombre coincide con una prop del componente real CategoryDetail.astro,
// para que el lector vincule diseño ↔ código sin saltos.
const partes = [
  {
    n: 1,
    nombre: 'Eyebrow + título (H2)',
    que: 'La etiqueta corta con barra de acento («La categoría a fondo», «El servicio a fondo») y el título del bloque, que es un H2 por defecto. La pareja eyebrow + título ubica al visitante en una palabra antes de leer el bloque y mantiene la jerarquía semántica del sitio (H1 del hero → H2 de cada sección a fondo).',
    dato: 'props eyebrow · title · as',
  },
  {
    n: 2,
    nombre: 'Body (párrafos)',
    que: 'Un array de párrafos que amplía lo que la tarjeta de la vitrina solo deja insinuado: el qué, el por qué y el «con qué se hace». Cada string del array es un <p> propio —el componente itera y los renderiza con margen entre ellos—, así que la página decide la longitud sin tocar el componente.',
    dato: 'prop body[]',
  },
  {
    n: 3,
    nombre: 'Puntos clave (lista con check)',
    que: 'La lista de bullets con check pintado en color de marca. Son los 3-4 datos sobre los que se quiere dejar caer la mirada —«Entrega 24 h», «Certificado», «Datos desde site.ts»—. Su trabajo es resumir, no repetir el body: títulos cortos, escaneables. Es opcional; sin puntos, la lista no se renderiza.',
    dato: 'prop points[]',
  },
  {
    n: 4,
    nombre: 'CTA + galería (info y apoyo)',
    que: 'El botón al final de la info («Ver categoría», «Ver módulo») y, en la columna derecha, la galería de apoyo —una imagen grande arriba y dos pequeñas debajo—. El CTA es opcional (los módulos «próximos» se documentan sin enlace); la galería es obligatoria: una imagen principal y un array de thumbs (puede ir vacío).',
    dato: 'props cta · gallery (main + thumbs[])',
  },
]

const beneficios = [
  {
    titulo: 'Amplía sin abandonar la lista',
    desc: 'La vitrina de tarjetas (CategoryCard) entrega el catálogo de un vistazo, pero cada tarjeta solo cabe 1-2 frases. Cuando hace falta vender la categoría —y no solo enseñarla— el bloque a fondo entra justo aquí: párrafos largos, puntos clave y galería sin pedirle al visitante que entre a otra página.',
  },
  {
    titulo: 'Texto y galería trabajando juntos',
    desc: 'La columna izquierda lleva la promesa en palabras; la derecha la refuerza en imágenes. Una imagen grande arriba (4:3) y dos pequeñas debajo dan contexto sin abrumar, y la jerarquía visual es siempre la misma: la información pesa, la galería ilustra. Nunca al revés.',
  },
  {
    titulo: 'Un solo molde para todas las páginas',
    desc: 'En la home cierra el catálogo, en /modulos amplía cada pieza, en /servicios podría ampliar cada servicio. Todos se ven idénticos —info izq · galería der— porque reusan el mismo componente con su misma estructura, sin variantes de presentación que confundan al lector.',
  },
]

const sis = [
  'Mantén SIEMPRE el orden info izq · galería der: la regla dura del sitio es «todos idénticos, sin zig-zag».',
  'Reusa CategoryDetail desde una fuente de datos (AFONDO[slug], TAXONOMY o una colección): data-driven, no hard-coded.',
  'Escribe el body en 2 párrafos máximo —concepto y por qué—; el detalle fino va en los puntos clave.',
  'Limita los puntos clave a 3-5 ideas escaneables («Datos desde site.ts», «Sin correo», «Schema centralizado»).',
  'Declara alt descriptivos por imagen (no «imagen1.jpg»); el componente ya añade width/height para evitar CLS.',
]
const nos = [
  'NO uses la prop reverse para alternar lados en una pila de bloques: rompe la regla de zig-zag del sitio.',
  'NO sustituyas el body por la galería: la imagen ilustra, no carga el peso narrativo.',
  'NO repitas en los puntos lo que ya dice el body: los puntos son el resumen, no el sub-índice.',
  'NO enlaces el CTA si la categoría aún no tiene página propia: omite la prop cta para no generar 404s.',
  'NO metas un H1 dentro: el H1 vive en el hero, el componente emite H2 por defecto (H3 con as="h3").',
]

// Variantes del bloque a fondo (galería). Réplica en vivo abajo, mismo orden.
// Las primeras son configuraciones REALES del componente actual (omitir cta,
// omitir points, bajar a H3, antipatrón documentado); las últimas son
// EXTENSIONES que requieren añadir CSS o una prop al componente.
const disenos = [
  {
    nombre: 'Canónica: info + puntos + CTA + galería',
    tag: 'Catálogo · Módulos · Servicios',
    desc: 'La de esta plantilla. Eyebrow + título (H2) + 2 párrafos de body + lista de puntos con check + CTA al final, y a la derecha galería con una imagen grande y dos pequeñas. Lo que se usa en la home (catálogo) y en /modulos (cada módulo a fondo). Configuración real: todas las props llenas.',
  },
  {
    nombre: 'Sin CTA (documentación · módulos próximos)',
    tag: 'Roadmap · Módulos «proximo»',
    desc: 'Misma estructura pero omitiendo la prop cta: la columna de info queda con el body y los puntos sin botón al final. Es la configuración que usa /modulos para los bloques «proximo» —documentan sin enlazar a una página inexistente—. Configuración real: omitir cta. Ya en producción en /modulos.',
  },
  {
    nombre: 'Sin puntos (solo body + CTA)',
    tag: 'Servicios cortos · Productos sin lista',
    desc: 'Cuando la categoría se explica con narrativa pura y no admite bullets útiles, se omiten los puntos: el body y el CTA quedan, la galería al lado. Útil para servicios consultivos o productos cuya promesa es una historia, no una lista. Configuración real: omitir points (o pasar []).',
  },
  {
    nombre: 'Galería de 1 imagen (sin thumbs)',
    tag: 'Editorial · Casos de estudio',
    desc: 'La galería admite un array de thumbs vacío: aparece solo la imagen grande de 4:3, sin la fila de dos pequeñas debajo. Pensado para piezas editoriales (un caso de éxito, un producto con una sola foto fuerte) donde forzar miniaturas debilita el bloque. Configuración real: gallery.thumbs = [].',
  },
  {
    nombre: 'Jerarquía H3 (sub-bloque)',
    tag: 'Listados anidados · Sub-secciones',
    desc: 'Cuando una sección H2 lleva varios bloques a fondo (p. ej. un servicio con sub-servicios), se reusa el componente bajando el título a H3 con la prop as="h3". Mantiene el mismo lenguaje visual sin romper la jerarquía semántica (H2 sección padre → H3 sub-bloque). Configuración real: as="h3".',
  },
  {
    nombre: 'Sobre fondo oscuro (extensión)',
    tag: 'CTA banner · Hero secundario',
    desc: 'Extensión propuesta: misma estructura pero con fondo oscuro y texto claro, para colocar el bloque sobre una sección destacada o un hero secundario. Hoy no existe la prop dark —similar a la de SectionHeading—. Implementarla requeriría derivar los colores del título, el body, los puntos y el border desde la variante.',
  },
]

// ── Recetas de implementación — strings que pinta <Receta> ───────────────────
// Las tres formas en que este bloque se usa en el sitio: uso básico (hard-
// coded para una página suelta), data-driven desde un map (como hace
// /modulos/index.astro con AFONDO[slug]) y reuso desde el catálogo (SHOWCASE)
// para no duplicar el título ni la imagen entre la tarjeta y el bloque a fondo.
const recetaUso = `---
// USO BÁSICO · pásale las 4 props mínimas (eyebrow, title, body,
// gallery) y listo. La galería siempre lleva una main; los thumbs
// pueden ir vacíos. El componente se encarga del CLS (width/height),
// del lazy de las imágenes y del 2-col → 1-col en móvil.
import CategoryDetail from '@components/CategoryDetail.astro'
---

<section class="section">
  <div class="container">
    <CategoryDetail
      eyebrow="La categoría a fondo"
      title="Cascos de seguridad"
      body={[
        'Cascos homologados para industria pesada, ligeros y con barboquejo ajustable. Stock para entrega 24 h en CDMX y zona metropolitana, garantía de fábrica.',
        'Reposición de piezas, asesoría de uso y capacitación opcional para equipos grandes —el casco se cambia cuando toca, no cuando se ve gastado—.',
      ]}
      points={[
        'Homologación NOM-115-STPS y EN 397',
        'Entrega 24 h en CDMX (stock permanente)',
        'Reposición de barboquejo y suspensión',
        'Capacitación opcional para equipos grandes',
      ]}
      cta={{ label: 'Ver catálogo de cascos', href: '/productos/cascos' }}
      gallery={{
        main:   { src: '/images/showcase/cascos-seguridad-industrial.avif', alt: 'Cascos de seguridad industrial certificados, vista de catálogo' },
        thumbs: [
          { src: '/images/productos/cascos-construccion.avif',  alt: 'Cascos para obra de construcción' },
          { src: '/images/productos/cascos-accesorios.avif',    alt: 'Accesorios para cascos: barboquejo y suspensión' },
        ],
      }}
    />
  </div>
</section>`

const recetaAfondo = `---
// DATA-DRIVEN DESDE UN MAP · cómo lo hace /modulos/index.astro.
// Una sola fuente (AFONDO en la página) alimenta los N bloques.
// Recorrer el array MODULOS de site.ts y por cada slug pintar su
// CategoryDetail: agregar un módulo a la SSoT se refleja aquí
// sin tocar el JSX.
import CategoryDetail from '@components/CategoryDetail.astro'
import { MODULOS } from '@config/site'

const AFONDO: Record<string, { body: string[]; points: string[] }> = {
  'topbar':  { body: [/* … */], points: [/* … */] },
  'header':  { body: [/* … */], points: [/* … */] },
  // …un objeto por cada módulo
}
const POOL = ['/images/showcase/a.avif', '/images/productos/b.avif', /* … */]
---

<div class="afondo">
  {MODULOS.map((m, i) => (
    <CategoryDetail
      eyebrow="El módulo a fondo"
      title={m.label}
      body={AFONDO[m.slug].body}
      points={AFONDO[m.slug].points}
      cta={m.estado === 'listo' ? { label: \`Ver el módulo \${m.label}\`, href: m.href } : undefined}
      gallery={{
        main:   { src: \`/images/showcase/\${m.slug}.avif\`, alt: \`Vista del módulo \${m.label}\` },
        thumbs: [
          { src: POOL[(i + 1) % POOL.length], alt: \`Detalle de \${m.label}\` },
          { src: POOL[(i + 2) % POOL.length], alt: \`Otro detalle de \${m.label}\` },
        ],
      }}
    />
  ))}
</div>

<style>
  /* Regla del sitio: SIN zig-zag, gap uniforme entre bloques. */
  .afondo { display: grid; gap: clamp(2.5rem, 6vw, 5rem); margin-top: var(--sp-6); }
</style>`

const recetaCatalogo = `---
// REUSO DESDE EL CATÁLOGO · cómo cerrar la home sin duplicar datos.
// La vitrina (SHOWCASE) ya tiene cada categoría con su image, label,
// blurb y subcategorías. El bloque a fondo reusa label como title e
// image como gallery.main, y solo agrega el body largo y los puntos.
import CategoryDetail from '@components/CategoryDetail.astro'
import { SHOWCASE } from '@config/site'

// El body extendido y los puntos viven aparte (no caben en SHOWCASE,
// que se diseñó para la tarjeta corta). Map por slug:
const EXTENDIDO: Record<string, { body: string[]; points: string[] }> = {
  'cascos':   { body: [/* … */], points: [/* … */] },
  'botas':    { body: [/* … */], points: [/* … */] },
  // …una entrada por categoría destacada
}
---

<div class="afondo">
  {SHOWCASE.filter((c) => EXTENDIDO[c.slug]).map((c) => (
    <CategoryDetail
      eyebrow="La categoría a fondo"
      title={c.label}
      body={EXTENDIDO[c.slug].body}
      points={EXTENDIDO[c.slug].points}
      cta={{ label: \`Ver catálogo de \${c.label}\`, href: c.href }}
      gallery={{
        main:   { src: c.image, alt: \`Catálogo de \${c.label}\` },
        thumbs: c.subcategories.slice(0, 2).map((s) => ({
          src: s.image, alt: \`Subcategoría \${s.label}\`,
        })),
      }}
    />
  ))}
</div>`

// ── Recetas para los patrones móviles (apartado «Responsive y móvil») ─────────
const recetaStack = `/* MÓVIL · DE DOS COLUMNAS A UNA (lo que ya hace el componente)
   El bloque es grid de 1fr 1fr en escritorio; bajo 768 px se
   colapsa a 1 columna —info arriba, galería debajo— para que el
   texto no se aplaste. Cero JS: el patrón está en el propio
   componente, no hace falta media query en la página padre. */

.cdetail {
  display: grid;
  grid-template-columns: 1fr;       /* móvil: una columna */
  gap: var(--sp-6);
  align-items: center;
}

@media (min-width: 768px) {
  .cdetail {
    grid-template-columns: 1fr 1fr;   /* escritorio: dos columnas */
    gap: clamp(2rem, 5vw, 4rem);      /* gap fluido entre columnas */
  }
}`

const recetaThumbs = `/* MÓVIL · LOS THUMBS SE QUEDAN EN 2 COLUMNAS (lo que ya hace el componente)
   La galería usa una imagen grande arriba (4:3) y dos pequeñas
   debajo en una rejilla 1fr 1fr. Esa rejilla NO se colapsa en
   móvil: dos thumbs lado a lado se siguen viendo bien, y apilarlos
   en columna alargaría demasiado el bloque. Excepción a la regla
   general «todo se apila en móvil»: aquí la pareja es lo que
   compacta el bloque. */

.cdetail__thumbs {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: var(--sp-3);
}
.cdetail__thumb {
  border-radius: var(--radius-md);
  aspect-ratio: 4 / 3;
  box-shadow: var(--shadow-sm);
}

/* La main image también mantiene su 4:3 con border-radius más
   grande —jerarquía visual: grande arriba, pareja debajo—. */
.cdetail__main {
  border-radius: var(--radius-lg);
  aspect-ratio: 4 / 3;
  box-shadow: var(--shadow-md);
}`

const recetaSpacing = `/* MÓVIL · ESPACIADO COMPRIMIDO ENTRE BLOQUES (extensión)
   Cuando hay 4+ CategoryDetail apilados (p. ej. /modulos),
   el clamp() default (2.5rem → 5rem) se siente excesivo en
   pantallas pequeñas. Esta extensión OPCIONAL lo reduce a
   1.5rem en ≤ 480 px sin tocar el componente. */

.afondo {
  display: grid;
  gap: clamp(2.5rem, 6vw, 5rem);  /* default escritorio */
}

@media (max-width: 480px) {
  .afondo { gap: var(--sp-6); }    /* 1.5rem en móvil */
}

/* Y para que la info no se sienta pegada a la galería del
   bloque anterior en pantallas chicas, el gap interno también
   se reduce un pelín. */
@media (max-width: 480px) {
  .cdetail { gap: var(--sp-4); }
}`

const recetaAccordion = `/* MÓVIL · GALERÍA COLAPSABLE CON <details> (extensión)
   En vitrinas con 6+ bloques apilados, la galería de 3 imágenes
   por bloque hace que la pantalla se sienta «pesada». Esta
   extensión envuelve la galería en un <details> nativo —cerrada
   por defecto en móvil—, para que la info se lea primero y la
   galería expanda solo si el visitante quiere verla.

   Requiere un cambio en el componente (envolver el div.cdetail__gallery
   en un <details> condicional por viewport o por prop), o un
   wrapper en la página padre. Se documenta como antipatrón
   suave: rompe la regla «todos idénticos» y solo conviene en
   landings muy largas. */

@media (max-width: 640px) {
  details.cdetail__galwrap {
    margin-top: var(--sp-3);
  }
  details.cdetail__galwrap > summary {
    cursor: pointer;
    font-family: var(--font-heading);
    font-weight: var(--weight-semibold);
    font-size: var(--text-sm);
    color: var(--c-primary);
    padding: var(--sp-2) 0;
    list-style: none;          /* oculta el triángulo nativo */
  }
  details.cdetail__galwrap > summary::after {
    content: ' ▾';
    transition: transform var(--transition);
  }
  details.cdetail__galwrap[open] > summary::after { content: ' ▴'; }
}

/* En escritorio, el <details> se ignora y la galería se ve siempre. */
@media (min-width: 641px) {
  details.cdetail__galwrap > summary { display: none; }
  details.cdetail__galwrap[open],
  details.cdetail__galwrap        { display: block; }
}`
import RelatedLinks from '@components/RelatedLinks.astro'
---

<PageLayout
  title="La Categoría a fondo — bloque de dos columnas con galería"
  description="Categoría a fondo: bloque de 2 columnas (info + galería) que amplía una categoría. Componente reutilizado en home, módulos y servicios, sin zig-zag."
  pageType="page"
  schemaType="TechArticle"
  guia={false}
  breadcrumbs={[{ label: 'Módulos', href: '/modulos' }, { label: 'Categoría a fondo' }]}
>
  <Hero
    badge="Módulo del sitio · Categoría a fondo"
    title="La Categoría a fondo:"
    accent="el bloque que profundiza"
    subtitle="El módulo de dos columnas que amplía UNA categoría justo donde la tarjeta no alcanza: a la izquierda la información detallada —eyebrow, título (H2), párrafos largos, puntos clave y CTA—; a la derecha una galería con una imagen grande y dos pequeñas para ilustrar."
    descRight={[
      'Esta página no es una ficha técnica: es el módulo entero, abierto y explicado. Qué problema resuelve y por qué se ganó su lugar al cierre del catálogo y en cada módulo de la serie, de qué piezas se compone, cómo se comporta en el teléfono, dónde encaja en el armado de la página y, al final, cómo está construido por dentro —del criterio de diseño a la línea de código—.',
      'Con una particularidad que conviene marcar desde el principio: la regla del sitio es dura. Todos los bloques «a fondo» van IDÉNTICOS —info a la izquierda, galería a la derecha—, sin alternar lados ni fondos. La prop reverse del componente existe como antipatrón documentado para no romper proyectos que la necesiten, no para usarse aquí. Coherencia > variedad.',
    ]}
  />

  {/* ── 1) ¿QUÉ ES? ── */}
  <ModSection
    eyebrow="Definición"
    title="¿Qué es la categoría a fondo?"
    desc="El bloque de dos columnas que amplía UNA categoría: a la izquierda la información detallada (eyebrow, título H2, párrafos, puntos clave y CTA); a la derecha una galería con una imagen grande arriba y dos pequeñas debajo."
    body={[
      'La categoría a fondo (o «category detail») es el módulo que toma una categoría —un grupo de productos, un servicio, un módulo de la serie— y le da el espacio para explicarse: no solo la frase corta de la tarjeta, sino el cuerpo entero, los datos clave y el contexto visual. No es un duplicado de la tarjeta: es lo que sigue, justo donde la tarjeta no alcanza.',
      'En este proyecto vive en un único componente, CategoryDetail.astro, con una API pequeña (id, eyebrow, title, body, points, cta, gallery, reverse, as). El bloque que estás leyendo ahora mismo, en /modulos/index → «cada módulo, por dentro», es exactamente este componente; y la regla del sitio es estricta —todos los bloques se ven idénticos, info a la izquierda y galería a la derecha, sin variantes que confundan la lectura—.',
    ]}
  />

  {/* ── 2) ¿PARA QUÉ SIRVE? ── */}
  <ModSection
    surface
    eyebrow="Función e importancia"
    title="¿Para qué sirve?"
    desc="Hace tres trabajos a la vez: convierte una tarjeta corta en un bloque que vende, junta texto y galería en un mismo movimiento de lectura, y mantiene el ritmo visual del sitio sin pedir CSS por categoría."
    body={[
      'Su función es cubrir el hueco entre la tarjeta de catálogo y la página de detalle. La tarjeta abre la puerta —imagen, etiqueta, dos frases—; el bloque a fondo la atraviesa: párrafos largos para vender, puntos clave para escanear y galería para ilustrar. Cuando la categoría se merece más de un vistazo, este es el módulo. La diferencia se mide en visitantes que no se pierden a la página interna sin haber entendido por qué deberían entrar.',
      'Y por eso pesa fuera de proporción a su altura. Para los buscadores, el bloque concentra la promesa de la categoría junto a sus imágenes con alt descriptivo —contenido sustantivo en un solo lugar—; para la accesibilidad, el H2 mantiene la jerarquía y los checks de los puntos son decoración (aria-hidden), no contenido; para el equipo que mantiene el sitio, un solo bloque para todas las categorías significa que ajustar la tipografía o el radio de la imagen se hace en un archivo y se propaga.',
    ]}
  >
    <ModBenefits items={beneficios} />
  </ModSection>

  {/* ── 3) ¿QUÉ LLEVA? — anatomía con ejemplo en vivo ── */}
  <ModSection
    eyebrow="Anatomía"
    title="¿Qué lleva el bloque?"
    desc="Cuatro piezas: el eyebrow + título (H2) que abre, el body de párrafos que amplía, los puntos clave con check que resumen, y el CTA + galería que cierran y refuerzan."
    body={[
      'Cada pieza cumple un papel claro. El eyebrow con barra de acento ubica de un vistazo («La categoría a fondo», «El módulo a fondo») y el título H2 abre el bloque con la jerarquía correcta. El body es el cuerpo narrativo en 1-3 párrafos; los puntos clave son el resumen en 3-5 bullets con check de color de marca; el CTA es la sola acción del bloque, anclado al final de la columna izquierda; y la galería —una imagen grande y dos pequeñas— ilustra al lado, en proporción y jerarquía pareja.',
      'Abajo, el ejemplo en vivo —réplica anotada a escala del componente—. Cada punto numerado se desglosa en su tarjeta: qué resuelve y de qué prop del componente sale. Y para que el mapeo sea inequívoco, los pines del espécimen apuntan a los mismos elementos del CategoryDetail que aparece en /modulos: lo que ves arriba es exactamente lo que se documenta abajo.',
    ]}
  >
    <ModAnatomy partes={partes}>
      {/* Ejemplo en vivo: réplica anotada a escala del componente (estructura
          idéntica al .cdetail del componente real, con pines numerados). */}
      <div class="cdx" role="img" aria-label="Ejemplo de categoría a fondo: info a la izquierda, galería a la derecha">
        <div class="cdx__info">
          <p class="cdx__eyebrow">
            <span class="cdx__bar" aria-hidden="true"></span>
            <span class="cdx__n cdx__n--inline">1</span>
            La categoría a fondo
          </p>
          <p class="cdx__title">Título de la categoría</p>
          <p class="cdx__p">
            <span class="cdx__n cdx__n--mid">2</span>
            Primer párrafo del body: lo que la tarjeta no alcanzó a decir. Hasta aquí cabe el cuerpo narrativo de la categoría.
          </p>
          <p class="cdx__p">Segundo párrafo: matices, casos o nota técnica de cierre.</p>
          <ul class="cdx__points" aria-label="Puntos clave">
            <li class="cdx__point"><span class="cdx__check" aria-hidden="true">✓</span>
              <span><span class="cdx__n cdx__n--mid">3</span>Punto clave A</span>
            </li>
            <li class="cdx__point"><span class="cdx__check" aria-hidden="true">✓</span><span>Punto clave B</span></li>
            <li class="cdx__point"><span class="cdx__check" aria-hidden="true">✓</span><span>Punto clave C</span></li>
          </ul>
          <span class="cdx__cta">
            <span class="cdx__n cdx__n--mid">4</span>
            Ver categoría →
          </span>
        </div>
        <div class="cdx__gallery">
          <span class="cdx__main" aria-hidden="true"></span>
          <div class="cdx__thumbs">
            <span class="cdx__thumb" aria-hidden="true"></span>
            <span class="cdx__thumb" aria-hidden="true"></span>
          </div>
        </div>
      </div>
    </ModAnatomy>
  </ModSection>

  {/* ── 3b) VARIANTES ── */}
  <ModSection
    surface
    eyebrow="Variantes"
    title="Otros diseños y aplicaciones"
    desc="La de esta plantilla es la canónica (info + puntos + CTA + galería), pero el bloque cambia de cara según lo que la página padre quiera ampliar: catálogo, roadmap, servicio corto, caso editorial, sub-sección."
    body={[
      'No hay un único modelo: hay una misma idea —info a la izquierda, galería a la derecha— que cada tipo de bloque ajusta. Un módulo en roadmap omite el CTA (no enlaza a una página inexistente); un servicio corto omite los puntos (la historia es la prueba); un caso editorial deja solo la imagen grande sin thumbs; una sub-sección baja la jerarquía a H3.',
      'Abajo, seis variantes —cinco son configuraciones REALES del componente actual (omitiendo cta, points, thumbs o cambiando as, más la prop reverse documentada como antipatrón), y una es una extensión propuesta que requeriría añadir una prop al componente—. Cada una con su réplica en vivo y el tipo de proyecto donde rinde mejor.',
    ]}
  >
      <GaleriaDisenos ariaLabel="Variantes de categoría a fondo">
        {/* 0 · Canónica */}
        <DisenoCard {...disenos[0]}>
          <div class="cdv" role="img" aria-label="Bloque canónico: info a la izquierda, galería a la derecha">
            <div class="cdv__info">
              <span class="cdv__eye">La categoría a fondo</span>
              <span class="cdv__t">Cascos de seguridad</span>
              <span class="cdv__d">Body de la categoría, en 1-2 frases.</span>
              <span class="cdv__pts"><i>✓ Punto A</i><i>✓ Punto B</i></span>
              <span class="cdv__cta">Ver catálogo →</span>
            </div>
            <div class="cdv__gal">
              <span class="cdv__main"></span>
              <span class="cdv__row"><i></i><i></i></span>
            </div>
          </div>
        </DisenoCard>

        {/* 1 · Sin CTA */}
        <DisenoCard {...disenos[1]}>
          <div class="cdv" role="img" aria-label="Bloque sin CTA: info, puntos y galería, sin botón">
            <div class="cdv__info">
              <span class="cdv__eye">El módulo a fondo</span>
              <span class="cdv__t">Módulo en construcción</span>
              <span class="cdv__d">No enlaza a una página que aún no existe.</span>
              <span class="cdv__pts"><i>✓ Punto A</i><i>✓ Punto B</i></span>
            </div>
            <div class="cdv__gal">
              <span class="cdv__main"></span>
              <span class="cdv__row"><i></i><i></i></span>
            </div>
          </div>
        </DisenoCard>

        {/* 2 · Sin puntos */}
        <DisenoCard {...disenos[2]}>
          <div class="cdv" role="img" aria-label="Bloque sin puntos clave: solo body y CTA">
            <div class="cdv__info">
              <span class="cdv__eye">El servicio a fondo</span>
              <span class="cdv__t">Consultoría a medida</span>
              <span class="cdv__d">Body amplio que cuenta la historia, sin bullets.</span>
              <span class="cdv__d">Cierre narrativo del servicio en una segunda línea.</span>
              <span class="cdv__cta">Cotizar →</span>
            </div>
            <div class="cdv__gal">
              <span class="cdv__main"></span>
              <span class="cdv__row"><i></i><i></i></span>
            </div>
          </div>
        </DisenoCard>

        {/* 3 · Sin thumbs */}
        <DisenoCard {...disenos[3]}>
          <div class="cdv" role="img" aria-label="Bloque con galería de una sola imagen, sin miniaturas">
            <div class="cdv__info">
              <span class="cdv__eye">Caso de éxito</span>
              <span class="cdv__t">Editorial · una sola foto</span>
              <span class="cdv__d">Body editorial, sin miniaturas.</span>
              <span class="cdv__pts"><i>✓ Punto A</i></span>
              <span class="cdv__cta">Leer caso →</span>
            </div>
            <div class="cdv__gal cdv__gal--nothumbs">
              <span class="cdv__main cdv__main--tall"></span>
            </div>
          </div>
        </DisenoCard>

        {/* 4 · Jerarquía H3 */}
        <DisenoCard {...disenos[4]}>
          <div class="cdv" role="img" aria-label="Bloque con título en jerarquía H3 (sub-sección)">
            <div class="cdv__info">
              <span class="cdv__eye">Sub-servicio</span>
              <span class="cdv__t cdv__t--sm">Sub-bloque dentro de un H2</span>
              <span class="cdv__d">Mismo bloque, otra etiqueta semántica.</span>
              <span class="cdv__pts"><i>✓ Punto A</i></span>
              <span class="cdv__cta">Ver sub →</span>
            </div>
            <div class="cdv__gal">
              <span class="cdv__main"></span>
              <span class="cdv__row"><i></i><i></i></span>
            </div>
          </div>
        </DisenoCard>

        {/* 5 · Oscura (extensión) */}
        <DisenoCard {...disenos[5]}>
          <div class="cdv cdv--dark" role="img" aria-label="Bloque sobre fondo oscuro (extensión propuesta)">
            <div class="cdv__info">
              <span class="cdv__eye">CTA banner</span>
              <span class="cdv__t">Sobre fondo oscuro</span>
              <span class="cdv__d">Mismo bloque, paleta invertida.</span>
              <span class="cdv__pts"><i>✓ Punto A</i><i>✓ Punto B</i></span>
              <span class="cdv__cta">Empezar →</span>
            </div>
            <div class="cdv__gal">
              <span class="cdv__main"></span>
              <span class="cdv__row"><i></i><i></i></span>
            </div>
          </div>
        </DisenoCard>
      </GaleriaDisenos>

      <GuiaNota titulo="Configuraciones reales vs extensión propuesta · la prop reverse es antipatrón" archivo="src/components/CategoryDetail.astro">
        <p>
          Las variantes <strong>0–4</strong> son configuraciones <strong>reales</strong> del componente actual: se obtienen omitiendo
          <code>cta</code>, <code>points</code> o <code>thumbs</code>, o cambiando <code>as="h3"</code> —sin tocar el componente—. La variante
          <strong>5 (sobre fondo oscuro)</strong> es una <strong>extensión propuesta</strong>: hoy <code>CategoryDetail</code> no acepta un prop
          <code>dark</code>. Implementarla requeriría derivar los colores del título, el body, los puntos y el border desde la variante (similar a
          la prop <code>dark</code> de <code>SectionHeading</code>).
        </p>
        <p>
          Aparte: la prop <code>reverse</code> existe en la API (galería a la izquierda, info a la derecha) pero el sitio NO la usa. La regla canónica
          —<strong>todos los bloques a fondo idénticos, sin zig-zag</strong>— pesa más que la flexibilidad del componente. Se documenta como antipatrón
          para que nadie la active «por variedad» en un nuevo bloque sin entender por qué se descartó.
        </p>
      </GuiaNota>
  </ModSection>

  {/* ── 3c) RESPONSIVE Y MÓVIL ── */}
  <ModSection
    eyebrow="Responsive y móvil"
    title="Cómo se comporta en el teléfono"
    desc="El bloque no se encoge: cambia de modo. Las dos columnas se apilan info → galería, los thumbs se quedan en pareja, el espaciado entre bloques se comprime y la galería puede plegarse en un acordeón cuando la pila es larga."
    body={[
      'En escritorio el bloque vive cómodo como dos columnas (1fr 1fr) con un gap fluido. En el teléfono esas dos columnas no caben sin apretar el texto ni deformar las imágenes, así que el componente colapsa a una sola columna por defecto bajo 768 px —info arriba, galería debajo—. Cero JS: el patrón está en el propio componente, no hace falta media query en la página padre.',
      'A partir de ahí, hay dos detalles que conviene conocer (los thumbs se quedan en 2 columnas para no alargar el bloque y el espaciado entre bloques apilados se comprime en pantallas pequeñas) y una extensión opcional para landings muy largas: envolver la galería en un <details> nativo para que se abra solo cuando el visitante quiere verla. Las cuatro están abajo, cada una con su vista en el teléfono y una receta comentada lista para adaptar.',
    ]}
  >
    <ModResponsive>
      <ModPattern
        title="1 · De dos columnas a una (default)"
        desc="Es lo que ya hace el componente de fábrica. Bajo <code>768 px</code> el <code>grid-template-columns</code> pasa de <code>1fr 1fr</code> a <code>1fr</code> y la galería cae naturalmente debajo de la info, sin reordenar nada en JS. Cero esfuerzo del consumidor del componente."
        receta={{ titulo: 'CSS · stack 2 → 1 columna (default)', lang: 'css', code: recetaStack }}
      >
        <MarcoMovil ariaLabel="Bloque apilado en móvil: info arriba, galería debajo">
          <div class="cdm cdm--stack">
            <div class="cdm__info">
              <span class="cdm__eye">La categoría a fondo</span>
              <b class="cdm__t"></b>
              <span class="cdm__p"></span>
              <span class="cdm__cta">Ver categoría</span>
            </div>
            <div class="cdm__gal">
              <span class="cdm__main"></span>
              <span class="cdm__row"><i></i><i></i></span>
            </div>
          </div>
        </MarcoMovil>
      </ModPattern>

      <ModPattern
        title="2 · Los thumbs se quedan en pareja"
        desc="Excepción a la regla general «todo se apila en móvil»: la galería usa la imagen grande arriba (4:3) y dos pequeñas debajo en una rejilla <code>1fr 1fr</code> que NO se colapsa. Dos thumbs lado a lado se siguen viendo bien, y apilarlos en columna alargaría demasiado el bloque sin aportar."
        receta={{ titulo: 'CSS · thumbs en 2 columnas (componente)', lang: 'css', code: recetaThumbs }}
      >
        <MarcoMovil ariaLabel="Los dos thumbs se quedan lado a lado incluso en móvil">
          <div class="cdm cdm--thumbs">
            <div class="cdm__gal">
              <span class="cdm__main"></span>
              <span class="cdm__row"><i></i><i></i></span>
              <span class="cdm__hint" aria-hidden="true">↔ thumbs en 2 columnas, siempre</span>
            </div>
          </div>
        </MarcoMovil>
      </ModPattern>

      <ModPattern
        title="3 · Espaciado entre bloques comprimido (extensión)"
        desc="Cuando la pila tiene 4+ bloques (como en <code>/modulos</code>), el <code>clamp()</code> default (2.5rem → 5rem) se siente excesivo en pantallas chicas. Reducir el gap del wrapper <code>.afondo</code> a <code>var(--sp-6)</code> en ≤ 480 px ahorra altura sin sentirse apretado. Extensión local, fuera del componente."
        receta={{ titulo: 'CSS · gap reducido en ≤ 480 px', lang: 'css', code: recetaSpacing }}
      >
        <MarcoMovil ariaLabel="Pila de bloques con espaciado vertical comprimido en móvil">
          <div class="cdm cdm--tight">
            <div class="cdm__block">
              <span class="cdm__eye">Bloque A</span>
              <b class="cdm__t"></b>
              <span class="cdm__p"></span>
            </div>
            <div class="cdm__rule" aria-hidden="true"></div>
            <div class="cdm__block">
              <span class="cdm__eye">Bloque B</span>
              <b class="cdm__t"></b>
              <span class="cdm__p"></span>
            </div>
            <span class="cdm__hint" aria-hidden="true">↕ gap reducido en ≤ 480 px</span>
          </div>
        </MarcoMovil>
      </ModPattern>

      <ModPattern
        title="4 · Galería colapsable con <details> (extensión)"
        desc="En landings muy largas (6+ bloques apilados) la galería de 3 imágenes por bloque se siente pesada en móvil. Envolver la galería en un <code>&lt;details&gt;</code> nativo —cerrada por defecto— deja que la info se lea primero. Antipatrón suave: rompe la regla «todos idénticos», úsalo solo cuando la longitud justifique la fricción."
        receta={{ titulo: 'CSS · galería plegable con <details> nativo', lang: 'css', code: recetaAccordion }}
      >
        <MarcoMovil ariaLabel="Galería plegada en un acordeón nativo, cerrada por defecto">
          <div class="cdm cdm--acc">
            <div class="cdm__info">
              <span class="cdm__eye">La categoría a fondo</span>
              <b class="cdm__t"></b>
              <span class="cdm__p"></span>
              <span class="cdm__cta">Ver categoría</span>
            </div>
            <div class="cdm__acc">
              <span class="cdm__sum">Ver imágenes ▾</span>
            </div>
            <span class="cdm__hint" aria-hidden="true">▾ la galería se abre con un toque</span>
          </div>
        </MarcoMovil>
      </ModPattern>
    </ModResponsive>
  </ModSection>

  {/* ── 4) ¿DÓNDE VA? ── */}
  <ModSection
    surface
    eyebrow="Posición"
    title="¿Dónde se coloca?"
    desc="Una o varias veces por página, siempre dentro de un .container central, apilados en un wrapper .afondo con gap uniforme. En la home, al cierre del catálogo; en /modulos, uno por cada módulo; en /servicios, podría amplíar cada servicio."
    body={[
      'A diferencia de la tarjeta de categoría (N por rejilla) o del hero (uno por página), el bloque a fondo aparece tantas veces como categorías o piezas merezcan amplíarse. En la home cierra el catálogo con 2-3 categorías destacadas; en /modulos hay uno por cada módulo de la serie (15 hoy). El wrapper .afondo reparte la pila con un gap uniforme y la regla canónica del sitio es estricta: nunca alternando lados, nunca alternando fondos.',
      'Vive SIEMPRE dentro del .container central (90 % del ancho, max-width acotado). El bloque respeta el ancho del cuerpo y monta sobre la sección padre (.section o .section--surface) que aporta el padding vertical y el color de fondo. En escritorio, las dos columnas se reparten 1fr 1fr con un gap fluido; en el teléfono, se apilan con info arriba y galería debajo, todo dentro del mismo container.',
    ]}
  />

  {/* ── 5) CÓMO SE ARMA — recetas + prose técnica ── */}
  <ModSection
    eyebrow="Implementación"
    title="Cómo está construido"
    desc="Un único componente con API pequeña: id, eyebrow, title, body, points, cta, gallery, reverse, as. Devuelve un <div> con dos columnas (info izq, galería der), un Tag dinámico para el título (h2 por defecto, h3 si se pide) y la galería con width/height fijos para reservar el hueco (sin CLS)."
    body={[
      'El componente vive en CategoryDetail.astro y expone nueve props a propósito: id (anchor opcional), eyebrow (etiqueta corta con barra), title (obligatorio, el H2), body[] (párrafos), points[] (lista con check, opcional), cta { label, href, external? } (opcional), gallery { main, thumbs[] } (obligatorio), reverse (antipatrón documentado) y as (h2|h3). La regla de uso está cableada en los defaults: as="h2", reverse=false, body=[], points=[]; basta con pasar title y gallery para obtener un bloque válido.',
      'En el sitio, lo más común es alimentarlo desde un map por slug. La página /modulos define AFONDO[slug] con body + points por módulo, mapea MODULOS y por cada uno renderiza un CategoryDetail con el cta condicionado al estado «listo». Misma fórmula podría usarse desde SHOWCASE en la home o desde una Content Collection. Cero JavaScript en el componente: HTML + CSS estático generado en build, imágenes con loading=lazy y decoding=async.',
    ]}
  >
      <div class="recetas">
        <Receta titulo="Astro · uso básico (hard-coded, una sola categoría)" lang="astro" code={recetaUso} />
        <Receta titulo="Astro · data-driven desde AFONDO[slug] (cada módulo a fondo)" lang="astro" code={recetaAfondo} />
        <Receta titulo="Astro · reuso desde el catálogo (sin duplicar imagen ni título)" lang="astro" code={recetaCatalogo} />
      </div>

      <ModProse>
        <p>
          En concreto: el componente recibe las props, decide la etiqueta del título
          (<code>const Tag = as</code>) y emite un <code>&lt;div class="cdetail"&gt;</code> con
          dos sub-divs: <code>.cdetail__info</code> (eyebrow opcional, Tag con el título, los
          <code>&lt;p&gt;</code> del body, una <code>&lt;ul&gt;</code> de puntos si <code>points.length &gt; 0</code>
          y el <code>&lt;a&gt;</code> del CTA si la prop está) y <code>.cdetail__gallery</code> (un
          <code>&lt;figure&gt;</code> con la <code>main</code> y, si <code>thumbs.length &gt; 0</code>, una
          rejilla de dos <code>&lt;figure&gt;</code> pequeñas). Las imágenes llevan
          <code>width="800" height="600"</code> (main) y <code>width="400" height="300"</code> (thumbs)
          para reservar el hueco y evitar CLS, más <code>loading="lazy"</code> y <code>decoding="async"</code>.
        </p>
        <p>
          La accesibilidad es de fábrica: el contenedor es un <code>&lt;div&gt;</code> estructural,
          el título es un heading real (<code>&lt;h2&gt;</code> o <code>&lt;h3&gt;</code>), los puntos
          son una <code>&lt;ul&gt;</code> semántica con icono de check en <code>aria-hidden="true"</code>,
          y la flecha del CTA y la barra del eyebrow también van decorativas. El foco se ve
          (<code>:focus-visible</code> con outline de marca) y, para quienes prefieren menos
          animación (<code>prefers-reduced-motion</code>), la única transición —el gap del CTA—
          se desactiva. Los tokens del proyecto (<code>--c-primary</code>, <code>--radius-lg</code>,
          <code>--sp-6</code>) son la única fuente de estilo: cambiar el design system cambia
          todos los bloques a fondo del sitio.
        </p>
      </ModProse>
  </ModSection>

  {/* ── 6) BUENAS PRÁCTICAS ── */}
  <ModSection
    surface
    eyebrow="Buenas prácticas"
    title="Qué hacer y qué evitar"
    desc="La diferencia entre una pila de bloques a fondo que se lee con ritmo y una que se siente «hecha por bloques distintos» cabe en un puñado de hábitos —empezando por respetar la regla canónica del sitio: todos idénticos, sin zig-zag—."
    body={[
      'Ninguno de estos hábitos es capricho: salen de la regla canónica del proyecto («info izq · galería der, siempre»). El sitio se siente coherente porque cada bloque a fondo nace del mismo componente con la misma disposición, la misma tipografía y el mismo CTA. La única decisión real es la fuente de datos (un map por slug, SHOWCASE, una Content Collection) y qué partes opcionales activar (puntos, CTA, thumbs).',
      'La buena noticia es que casi todo se sostiene solo cuando se respeta el componente: el CLS lo cuidan los width/height de las imágenes, la jerarquía H2 está cableada por la prop as, y el responsive lo resuelve el componente (2 → 1 columna en móvil). Abajo, lo que conviene y lo que conviene evitar, enfrentados.',
    ]}
  >
    <ModDosDonts sis={sis} nos={nos} />
  </ModSection>

  {/* ── 7) EAT YOUR OWN DOG FOOD — el componente en vivo, no como mockup ── */}
  <ModSection
    eyebrow="En vivo"
    title="El componente, en su uso real"
    desc="Las galerías de variantes son mockups a escala; este bloque NO. Aquí se renderiza un CategoryDetail real con sus props llenas, idéntico a los que verás en /modulos."
    body={[
      'La regla del sitio es estricta —info izq · galería der, sin zig-zag, sin reverse—. Este bloque cierra la página con esa misma disposición canónica, alimentado por el componente real, no por una réplica anotada. Si el componente se rompe, este bloque se rompe a la vista; documentación que se documenta a sí misma.',
      'Las imágenes son las mismas fotos demo del proyecto (AVIF optimizadas, lazy, decoding async). El CTA enlaza al índice de la serie. Reutilizar las fotos del POOL evita inflar el assets de la plantilla: en un sitio real, cada categoría llevaría su propia galería.',
    ]}
  >
      <div class="afondo">
        <CategoryDetail
          eyebrow="La categoría a fondo · ejemplo real"
          title="Categoría a fondo, con el propio componente"
          body={[
            'Este bloque NO es un mockup: es el CategoryDetail real renderizándose, con las props llenas y la misma disposición canónica que el resto del sitio. Si tocas la API del componente, este bloque se rompe en /modulos/category-detail antes que en producción.',
            'La galería usa la misma fórmula del L2 (POOL de fotos demo) para no inflar el repositorio. En un sitio real cada categoría tendría su galería propia, con alt descriptivos por imagen y carga eager solo en la primera (LCP friendly) si el bloque está sobre el pliegue.',
          ]}
          points={[
            'Disposición canónica: info izquierda · galería derecha (sin reverse)',
            'Imágenes con width/height para reservar el hueco (cero CLS)',
            'Galería en grid 4:3, thumbs en 2 columnas también en móvil',
            'CTA opcional: omítelo cuando la categoría aún no tenga página',
          ]}
          cta={{ label: 'Ver todos los módulos', href: '/modulos' }}
          gallery={{
            main: { src: '/images/productos/sitio-web-rapido-astro.avif', alt: 'Vista de ejemplo del bloque CategoryDetail en producción' },
            thumbs: [
              { src: '/images/showcase/imagen-optimizada-avif-sitio-web.avif', alt: 'Detalle: imagen AVIF optimizada para el sitio' },
              { src: '/images/showcase/jerarquia-titulos-seo-pagina-web.avif', alt: 'Detalle: jerarquía de títulos para SEO' },
            ],
          }}
        />
      </div>
  </ModSection>

  {/* Cierre */}
  <SectionMenu items={cierreItems} cta={cierreCta} ariaLabel="Sigue explorando" />

  {/* Cross-linking interno (B6 PROYECTORED): 3 enlaces curados al final de la página. */}
  <RelatedLinks
    title="Continúa explorando"
    desc="Otros módulos del flujo que encajan con éste."
    links={[
      { label: 'Category Card', href: '/modulos/category-card', desc: 'La card de catálogo que lleva a este detalle.' },
      { label: 'Breadcrumbs', href: '/modulos/breadcrumbs', desc: 'Las migas que dicen «estás aquí, en esta categoría».' },
      { label: 'FAQ', href: '/modulos/faq', desc: 'Preguntas comunes — encajan al final del detalle.' },
    ]}
    limit={3}
  />
</PageLayout>

<style>
  /* SOLO mockups bespoke de esta página (.cdx/.cdv/.cdm) + .recetas + .afondo.
     El armazón estructural (.section/.container/.bene/.partes/.prose/.dodont/.pats/.pat)
     vive en el KIT (src/components/modulos/*) — antes estaba duplicado aquí. */

  /* ── Ejemplo en vivo del bloque (réplica a escala del componente) ── */
  .cdx {
    display: grid; grid-template-columns: 1fr; gap: var(--sp-5);
    background: var(--c-white); border: 1px solid var(--c-border);
    border-radius: var(--radius-lg); padding: var(--sp-5);
    margin: var(--sp-6) auto 0; max-width: 760px;
    box-shadow: 0 1px 3px rgba(0,0,0,.04);
  }
  @media (min-width: 700px) { .cdx { grid-template-columns: 1fr 1fr; align-items: center; gap: var(--sp-6); } }
  .cdx__info { display: flex; flex-direction: column; gap: var(--sp-3); }
  .cdx__eyebrow {
    display: inline-flex; align-items: center; gap: var(--sp-2); margin: 0;
    font-size: var(--text-xs); font-weight: var(--weight-bold);
    text-transform: uppercase; letter-spacing: .08em; color: var(--c-primary);
  }
  .cdx__bar { width: 18px; height: 2px; background: currentColor; border-radius: 2px; }
  .cdx__title {
    margin: 0; font-family: var(--font-heading);
    font-size: var(--text-xl); font-weight: var(--weight-black);
    line-height: var(--leading-tight); color: var(--c-ink);
  }
  .cdx__p { margin: 0; font-size: var(--text-sm); line-height: var(--leading-normal); color: var(--c-ink-2); position: relative; }
  .cdx__points { list-style: none; margin: var(--sp-1) 0 0; padding: 0; display: flex; flex-direction: column; gap: var(--sp-2); }
  .cdx__point { display: flex; align-items: flex-start; gap: var(--sp-2); font-size: var(--text-sm); color: var(--c-ink-2); }
  .cdx__check {
    flex-shrink: 0; display: grid; place-items: center; margin-top: .15em;
    width: 18px; height: 18px; border-radius: var(--radius-full);
    background: color-mix(in srgb, var(--c-primary) 14%, transparent); color: var(--c-primary);
    font-size: .65rem; font-weight: var(--weight-bold);
  }
  .cdx__cta {
    align-self: flex-start; margin-top: var(--sp-2);
    display: inline-flex; align-items: center; gap: var(--sp-2);
    padding: .55em 1em;
    font-family: var(--font-heading); font-weight: var(--weight-semibold);
    font-size: var(--text-sm); color: #fff;
    background: var(--c-primary); border-radius: var(--radius-md);
  }
  .cdx__gallery { display: flex; flex-direction: column; gap: var(--sp-3); }
  .cdx__main, .cdx__thumb {
    background: linear-gradient(135deg, var(--c-primary-light, #f3eefe), var(--c-primary));
    border-radius: var(--radius-md); aspect-ratio: 4 / 3;
  }
  .cdx__main { border-radius: var(--radius-lg); }
  .cdx__thumbs { display: grid; grid-template-columns: 1fr 1fr; gap: var(--sp-3); }
  .cdx__n {
    display: inline-grid; place-items: center; width: 18px; height: 18px;
    border-radius: var(--radius-full); background: var(--c-primary); color: #fff;
    font-family: var(--font-heading); font-weight: var(--weight-bold); font-size: .6875rem;
    flex-shrink: 0;
  }
  .cdx__n--mid { background: var(--c-muted); margin-right: 4px; }
  .cdx__n--inline { margin-right: 4px; vertical-align: middle; }

  /* ── Recetas (apiladas, ancho completo) ── */
  .recetas { display: grid; gap: var(--sp-4); margin-top: var(--sp-6); }

  /* ── Pila «a fondo» (CategoryDetail) — gap uniforme, SIN zig-zag (= home y L2) ── */
  .afondo { display: grid; gap: clamp(2.5rem, 6vw, 5rem); margin-top: var(--sp-6); }

  /* ── Mockups de variantes (dentro de <GaleriaDisenos>) ───────────────────── */
  .cdv {
    display: grid; grid-template-columns: 1fr 1fr; gap: var(--sp-3);
    width: 100%; padding: var(--sp-3);
    background: var(--c-white); border: 1px solid var(--c-border);
    border-radius: var(--radius-sm); font-size: 11px; color: var(--c-ink); min-height: 150px;
  }
  .cdv__info { display: flex; flex-direction: column; gap: 4px; }
  .cdv__eye { font-size: 9px; font-weight: 700; letter-spacing: .04em; text-transform: uppercase; color: var(--c-primary); }
  .cdv__t { font-family: var(--font-heading); font-weight: var(--weight-semibold); font-size: 12px; color: var(--c-ink); line-height: 1.2; }
  .cdv__t--sm { font-size: 10px; }
  .cdv__d { font-size: 9px; color: var(--c-muted); line-height: 1.35; }
  .cdv__pts { display: flex; flex-direction: column; gap: 2px; }
  .cdv__pts i { font-style: normal; font-size: 9px; color: var(--c-ink-2); }
  .cdv__cta {
    margin-top: auto; align-self: flex-start;
    font-size: 9px; font-weight: var(--weight-semibold); color: #fff;
    background: var(--c-primary); padding: 3px 7px; border-radius: var(--radius-sm);
  }
  .cdv__gal { display: flex; flex-direction: column; gap: 4px; }
  .cdv__main {
    aspect-ratio: 4 / 3; border-radius: 4px;
    background: linear-gradient(135deg, var(--c-primary-light, #f3eefe), var(--c-primary));
  }
  .cdv__main--tall { aspect-ratio: 4 / 5; }
  .cdv__row { display: grid; grid-template-columns: 1fr 1fr; gap: 4px; }
  .cdv__row i {
    font-style: normal; aspect-ratio: 4 / 3; border-radius: 3px;
    background: linear-gradient(135deg, var(--c-primary-light, #f3eefe), var(--c-primary)); opacity: .7;
  }
  .cdv__gal--nothumbs .cdv__main { aspect-ratio: 3 / 4; }
  /* Dark */
  .cdv--dark { background: var(--c-ink); color: #fff; }
  .cdv--dark .cdv__t { color: #fff; }
  .cdv--dark .cdv__d, .cdv--dark .cdv__pts i { color: rgba(255,255,255,.72); }
  .cdv--dark .cdv__eye { color: var(--c-primary-light, #f3eefe); }

  /* ── Mockups MÓVIL (dentro de <MarcoMovil>) ──────────────────────────────── */
  .cdm { padding: 10px; font-size: 11px; color: var(--c-ink); display: flex; flex-direction: column; gap: 8px; }
  .cdm__info, .cdm__block { display: flex; flex-direction: column; gap: 4px; padding: 8px; background: var(--c-white); border: 1px solid var(--c-border); border-radius: 7px; }
  .cdm__eye { font-size: 8px; font-weight: 700; letter-spacing: .04em; text-transform: uppercase; color: var(--c-primary); }
  .cdm__t { display: block; height: 9px; width: 65%; border-radius: 4px; background: var(--c-ink); opacity: .8; }
  .cdm__p { display: block; height: 6px; width: 92%; border-radius: 3px; background: var(--c-surface); border: 1px solid var(--c-border); }
  .cdm__cta { align-self: flex-start; margin-top: 2px; font-size: 9px; font-weight: 700; color: #fff; background: var(--c-primary); padding: 3px 7px; border-radius: 4px; }
  .cdm__gal { padding: 8px; background: var(--c-white); border: 1px solid var(--c-border); border-radius: 7px; display: flex; flex-direction: column; gap: 4px; }
  .cdm__main {
    display: block; height: 56px; border-radius: 5px;
    background: linear-gradient(135deg, var(--c-primary-light, #f3eefe), var(--c-primary));
  }
  .cdm__row { display: grid; grid-template-columns: 1fr 1fr; gap: 4px; }
  .cdm__row i {
    font-style: normal; display: block; aspect-ratio: 4 / 3; border-radius: 3px;
    background: linear-gradient(135deg, var(--c-primary-light, #f3eefe), var(--c-primary)); opacity: .65;
  }
  .cdm__hint { display: block; margin-top: 4px; font-size: 9px; color: var(--c-muted); text-align: center; }

  /* 3 · Espaciado comprimido */
  .cdm--tight { gap: 4px; }
  .cdm__rule { height: 1px; background: var(--c-border); margin: 2px 0; }

  /* 4 · Acordeón */
  .cdm__acc { padding: 6px 10px; background: var(--c-surface); border: 1px solid var(--c-border); border-radius: 6px; }
  .cdm__sum { font-size: 10px; font-weight: 700; color: var(--c-primary); }
</style>
